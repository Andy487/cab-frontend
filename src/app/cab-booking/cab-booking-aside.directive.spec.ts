import { CabBookingAsideDirective } from './cab-booking-aside.directive';

describe('CabBookingAsideDirective', () => {
  it('should create an instance', () => {
    const directive = new CabBookingAsideDirective();
    expect(directive).toBeTruthy();
  });
});
